#!/bin/bash
ruby -I. init_trajectory.rb --url $SIM_BASE_URL -u $SIM_USERNAME -p $SIM_PASSWORD --uidWell "$SIM_UID_WELL" --uidTraj "$SIM_UID_TRAJECTORY" --nameWell "$SIM_NAME_WELL" --nameTraj "$SIM_NAME_TRAJECTORY"
